# Sequence API

Sequence APIs convert scheduled diaries into episodes and fixed-interval state strings, then compare those strings with optimal matching.

```{eval-rst}
.. automodule:: athenspop.sequence
   :members:
```
