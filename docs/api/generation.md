# Generation API

Generation helpers repeat the scheduler with derived seeds so users can sample multiple feasible realizations from the same validated dataset.

```{eval-rst}
.. automodule:: athenspop.generation
   :members:
```
